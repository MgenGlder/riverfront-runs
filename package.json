{
  "name": "riverfront-runs",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Landing page for Riverfront Run Detroit — a free weekly community run on the Detroit Riverfront, with an optional live runner map.",
  "scripts": {
    "dev": "concurrently -k -n web,ws -c cyan,magenta \"vite\" \"node server/index.js\"",
    "server": "node server/index.js",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node server/index.js"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-leaflet": "^4.2.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "concurrently": "^9.1.2",
    "vite": "^6.0.7"
  }
}
